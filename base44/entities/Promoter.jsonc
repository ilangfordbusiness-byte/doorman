{
  "name": "Promoter",
  "type": "object",
  "properties": {
    "event_id": {
      "type": "string",
      "description": "Reference to event"
    },
    "name": {
      "type": "string",
      "description": "Promoter display name"
    },
    "email": {
      "type": "string",
      "description": "Promoter contact email"
    },
    "commission_type": {
      "type": "string",
      "enum": [
        "flat",
        "percent"
      ],
      "default": "percent",
      "description": "flat = fixed fee per ticket, percent = percentage of ticket price"
    },
    "commission_value": {
      "type": "number",
      "description": "Flat fee per ticket (major units) if type=flat, or percentage of paid amount if type=percent"
    },
    "tracking_code": {
      "type": "string",
      "description": "Unique shareable tracking code used in the promoter link"
    },
    "status": {
      "type": "string",
      "enum": [
        "active",
        "disabled"
      ],
      "default": "active"
    },
    "tickets_sold": {
      "type": "number",
      "description": "Running count of tickets sold through this promoter's link",
      "default": 0
    },
    "total_sales": {
      "type": "number",
      "description": "Total ticket sales value attributed to this promoter, major units",
      "default": 0
    },
    "commission_owed": {
      "type": "number",
      "description": "Running total commission earned but not yet paid out, major units",
      "default": 0
    },
    "commission_paid": {
      "type": "number",
      "description": "Total commission marked as paid out to this promoter, major units",
      "default": 0
    },
    "clicks": {
      "type": "number",
      "description": "Total clicks on the promoter tracking link (counted once per guest session)",
      "default": 0
    },
    "discount_type": {
      "type": "string",
      "enum": [
        "none",
        "percent",
        "flat"
      ],
      "default": "none",
      "description": "Auto-applied guest discount on this promoter's link: none, percent off, or flat amount off"
    },
    "discount_value": {
      "type": "number",
      "description": "Discount percentage (0-100) if type=percent, or flat amount off (major units) if type=flat",
      "default": 0
    },
    "discount_max_uses": {
      "type": "number",
      "description": "Max discounted purchases via this link. 0 = unlimited. The link still works after the limit is hit; the discount simply stops applying.",
      "default": 0
    },
    "discount_used_count": {
      "type": "number",
      "description": "Number of discounted purchases made via this promoter's link",
      "default": 0
    },
    "discount_given": {
      "type": "number",
      "description": "Total discount amount given via this promoter's link, major units",
      "default": 0
    }
  },
  "required": [
    "event_id",
    "name",
    "commission_type",
    "commission_value",
    "tracking_code"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.email": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.email": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}